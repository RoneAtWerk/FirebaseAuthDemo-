# Firebase Auth Demo

This is a simple iOS SwiftUI app that uses Firebase Authentication.

## Features
- User signup with email and password
- User login
- Error handling for invalid credentials
- Navigation to home screen after login
- Logout functionality

## Technologies Used
- SwiftUI
- Firebase Authentication

## How to Run
1. Open the project in Xcode
2. Make sure Firebase is configured
3. Run on simulator
